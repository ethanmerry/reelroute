{
  "crons": [
    {
      "path": "/api/fetchMovies",
      "schedule": "0 0 * * *"  // Runs every day at midnight
    }
  ],
  "functions": {
    "api/fetchMovies": {
      "maxDuration": 60
    }
  }
}
